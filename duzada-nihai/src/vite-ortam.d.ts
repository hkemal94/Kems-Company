/// <reference types="vite/client" />

// Vite'ın özel içe aktarma soneklerinin (`?url`, `?worker`, `?raw`) tip
// tanımları buradan geliyor. MapLibre worker'ı `?worker&url` ile
// yükleniyor — bkz. `components/harita/duzadaArazi.ts`.
